{
  "name": "doubleshot-vue-nest-starter",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "dsr dev",
    "debug": "dsr debug",
    "build": "rimraf dist && dsr build"
  },
  "devDependencies": {
    "@doubleshot/runner": "^0.0.5"
  }
}
